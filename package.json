{
  "name": "aay-scraper",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler d1 migrations apply aay-lunch --local -e dev && wrangler dev -e dev",
    "start": "wrangler dev",
    "test": "vitest run",
    "test:file": "vitest run",
    "cf-typegen": "wrangler types",
    "lint": "biome check",
    "lint:fix": "biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "typescript": "^5.9.3",
    "vitest": "~3.0.9",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "date-fns": "^4.4.0",
    "date-fns-tz": "^3.2.0",
    "hono": "^4.13.1",
    "linkedom": "^0.18.13"
  }
}
